using TMPro;
using UnityEngine;
using Fusion;

public class UITimer : MonoBehaviour
{
    //public TMP_Text timerText;

    //private TimerObject timerObj;
    //private float findTimerInterval = 0.5f;
    //private float findTimerCounter = 0f;

    //void Update()
    //{
    //    if (timerText == null)
    //    {
    //        Debug.LogWarning("[UITimer] timerText not assigned!");
    //        return;
    //    }

    //    if (timerObj == null)
    //    {
    //        // tìm period để tránh FindObjectOfType mỗi frame
    //        findTimerCounter += Time.unscaledDeltaTime;
    //        if (findTimerCounter >= findTimerInterval)
    //        {
    //            findTimerCounter = 0f;
    //            timerObj = FindFirstObjectByType<TimerObject>();
    //            Debug.Log($"[UITimer] FindObjectOfType TimerObject -> {timerObj}");
    //        }
    //        return;
    //    }

    //    timerText.text = timerObj.GetTimerText();
    //}
}